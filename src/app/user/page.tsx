import { UserProfile } from "./_components/userProfile/component";

export default async function UserPage() {
  return (
    <main className="grid min-h-screen place-items-center">
      <UserProfile />
    </main>
  );
}
